hydra:
  job:
    name: bar-simplicity
  sweep:
    dir: outputs/${hydra.job.name}/${now:%Y%m%d}_${now:%H%M%S}
    subdir: ${hydra.job.num}
  launcher:
    n_jobs: 8

make_envs_func:
  _target_: envs.uci_health.bar_crawl.make_simplicity_components
  sig_mult: 0.15
  bias_level: 1.0
  poison_feature_idx: 34
  xgbc_kwargs:
    n_jobs: 1
eval_bsz: 500
plf:
  _target_: lightning.Fabric
  accelerator: cpu
